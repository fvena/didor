{
  "name": "Docs",
  "private": true,
  "scripts": {
    "clean": "rimraf node_modules && rimraf .nuxt && rimraf .turbo && rimraf .output  && rimraf dist",
    "prepare": "nuxt prepare",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "dev": "nuxt dev --port=3000",
    "preview": "nuxt preview",
    "postinstall": "npm run prepare"
  },
  "dependencies": {
    "@didor/docs": "*"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  }
}